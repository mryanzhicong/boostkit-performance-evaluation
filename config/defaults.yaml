architectures:
  - x86_64
  - aarch64

runner_labels:
  x86_64: PERF_RUNNER_X86_64
  aarch64: PERF_RUNNER_ARM64

output_root: .perf-output
work_root: /tmp/boostkit-perf

modes:
  smoke:
    timeout_minutes: 30
  full:
    timeout_minutes: 180
